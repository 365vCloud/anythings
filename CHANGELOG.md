# 更新迭代历史

本文件记录 Anythings 的主要功能迭代过程，时间顺序从旧到新。

## v0.1 - 项目初始化

- 参考 voidtools Everything 的检索理念，使用原生 SwiftUI 搭建 macOS 文件检索应用骨架。
- 实现用户自选文件夹索引、文件名/路径快速检索、通配符（`*`、`?`）与引号短语匹配、全路径匹配开关。
- 提供 Finder 集成：打开文件、在 Finder 中显示、复制路径。
- 编写 `scripts/package-dmg.sh` 打包脚本与 GitHub Actions "Build macOS DMG" 工作流，实现在 macOS Runner 上自动构建并产出 `.dmg` 安装包。

## v0.2 - 修复并发编译问题

- 修复 `FileScanner.scan` 中因 Swift 6 并发检查（`@escaping @Sendable` 闭包捕获 `self`）导致的编译失败问题，移除进度回调闭包，简化为无进度上报的扫描实现。
- 修正 `SearchExpression` 中 `switch-case` 多模式 `where` 子句只作用于最后一个分支的告警/编译问题。

## v0.3 - 添加应用图标

- 新增 `scripts/generate-icon.py`，使用纯 Python 标准库（无第三方依赖）程序化绘制放大镜风格的搜索图标，生成 10 种所需尺寸的 iconset。
- 打包脚本新增 `iconutil` 转换步骤，生成 `.icns` 并通过 `CFBundleIconFile` 关联到应用包，使 DMG 内应用带有专属图标。

## v0.4 - 修复检索无结果问题（Spotlight 兜底）

- 定位到原实现仅依赖本地全量扫描索引进行检索，扫描未完成或索引较大时检索结果无法及时反馈。
- 新增 `Sources/SpotlightSearch.swift`，通过 `mdfind` 调用 macOS Spotlight，对每个索引根目录并发查询，构造 `kMDItemFSName`/`kMDItemDisplayName`（或含 `kMDItemPath`）通配符谓词，查询前 250ms 防抖。
- 检索结果改为"本地扫描索引 + Spotlight 结果"合并去重，即使本地扫描未完成也能即时看到匹配项。

## v0.5 - 文件夹管理与系统文件过滤

- 新增删除已索引文件夹功能：文件夹列表每行增加垃圾桶按钮，并支持右键菜单删除（此前只能添加、无法移除）。
- 新增"隐藏系统/日志文件"开关（默认开启），自动过滤 `.log`、`.tmp`、`.cache`、`.bak`、`.DS_Store`、`Thumbs.db`、隐藏点文件等系统与临时文件，避免检索结果被噪音污染。

## v0.6 - 批量操作、视图与高级筛选

- 新增多选支持：全选、取消全选、反选（工具栏菜单、菜单栏 `Selection` 菜单，及快捷键 `⌘A` / `⇧⌘A` / `⌘I`）。
- 新增"复制到文件夹"、"移动到文件夹"操作，可对多选结果批量执行，移动完成后自动重新索引。
- 新增结果视图切换：详情列表（可排序表格）、大图标、小图标三种视图模式。
- 新增排序菜单：按名称、路径、修改日期、大小排序，支持升序/降序。
- 搜索结果右键菜单新增"打开所在目录"（Open Containing Folder），与原有"在 Finder 中显示"并存。
- 新增高级文件类型筛选器：内置 PDF、文档、图片、音频、视频、压缩包、代码、应用程序共 8 大类别复选框，并支持自定义扩展名输入（如 `psd, ai`）。

## v0.7 - 修复 Spotlight 检索单点故障

- 修复 Spotlight 检索中，只要有一个已索引文件夹变得不可访问（如外接盘被卸载、目录被移除），`mdfind` 调用失败即会中断整个检索循环、导致所有文件夹结果均返回为空的问题。
- 改为单个文件夹检索失败时仅记录日志并跳过，不影响其余文件夹正常返回结果。

## v0.8 - 修复搜索输入卡顿

- 定位到每次按键都会在主线程（UI 线程）同步执行"正则匹配 + 遍历全量索引 + 排序"的完整流程，索引越大、输入越卡顿。
- 将过滤与排序逻辑迁移至后台线程（`Task.detached`）执行，避免阻塞主线程与输入框；文本输入与自定义扩展名输入增加 150ms 防抖，减少高频按键触发的重复计算。
- 勾选类操作（切换文件夹、开关选项、排序方式切换等）保持即时响应，无需等待防抖。

## v1.0.1 - 安全与稳定性审查修复

- 完成安全审查（命令注入、正则注入、文件操作、打包脚本、CI 供应链），未发现可利用漏洞。
- 修复 Spotlight 检索在 `mdfind` 输出超过管道缓冲区（约 64KB，即结果较多）时的死锁挂死问题：改为先读取输出流至 EOF 再等待进程退出。
- 检索任务被取消时现在会终止对应的 `mdfind` 子进程，避免连续输入积累僵尸进程。
- 修复索引进行中删除文件夹后、该文件夹文件在扫描完成时重新出现的问题；同时清理其 Spotlight 缓存结果，并丢弃针对已变更文件夹列表的过期检索结果。
- 结果多选快捷键改为 `⌥⌘A` / `⌥⇧⌘A` / `⌥⌘I`，不再与搜索框文本全选（`⌘A`）冲突。
- 复制到已索引目录后自动刷新索引；禁止将文件夹复制/移动到其自身子目录。
- 错误弹窗标题按场景区分（索引错误 / 检索错误 / 复制失败 / 移动失败）。
