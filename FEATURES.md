# 功能描述

Anythings 是一款参考 [voidtools Everything](https://www.voidtools.com/zh-cn/support/everything/) 检索理念、面向 macOS 平台原生开发的文件检索工具。它使用 SwiftUI 与 AppKit 构建，为用户自选的文件夹建立本地索引，并结合 macOS Spotlight 提供即时检索，帮助你在海量文件中快速定位目标文件或文件夹。

## 核心检索能力

- **本地索引 + Spotlight 双引擎**：应用会对用户添加的文件夹建立本地文件名/路径索引，同时并行调用系统 `mdfind`（Spotlight）进行即时查询，两路结果合并去重后展示，兼顾"索引未完成时也能查到"和"索引完成后离线可用"两种场景。
- **Everything 风格的查询语法**：
  - 支持通配符 `*`（任意长度）与 `?`（单字符），例如 `report*.pdf`。
  - 支持英文引号包裹的短语匹配，例如 `"project report"`。
  - 支持空格分隔的多关键词组合（AND 逻辑）。
  - 可切换"仅匹配文件名"或"匹配完整路径"。
  - 可切换大小写敏感检索。
- **后台异步检索**：检索、过滤、排序均在后台线程执行，输入关键词时增加短暂防抖，避免因大索引导致输入框卡顿，保证界面流畅。
- **系统文件自动过滤**：默认自动隐藏 `.log`、`.tmp`、`.cache`、`.bak`、`.DS_Store`、`Thumbs.db`、隐藏点文件等系统/临时文件噪音，可在侧边栏一键关闭该过滤。
- **高级文件类型筛选**：内置 PDF、文档、图片、音频、视频、压缩包、代码、应用程序共 8 大类型分类复选框，并支持输入自定义扩展名（如 `psd, ai, sketch`），组合多个条件缩小检索范围。

## 索引与文件夹管理

- 可自由添加任意数量的文件夹作为索引根目录（多选文件夹一次性添加）。
- 支持单独移除某个已索引文件夹（列表内的垃圾桶按钮或右键菜单），移除后立即从索引与结果中剔除。
- 支持手动"重新索引"，刷新指定文件夹下的最新文件状态。
- 侧边栏实时显示"已索引项目数"与"当前匹配数"。

## 结果查看与交互

- **多种视图模式**：详情（可排序表格）、大图标、小图标，随时切换以适应不同浏览习惯。
- **多字段排序**：按名称、路径、修改日期、大小排序，支持升序/降序切换。
- **多选与批量操作**：支持全选、取消全选、反选（工具栏菜单 / 菜单栏 `Selection` 菜单 / 快捷键 `⌘A`、`⇧⌘A`、`⌘I`）。
- **Finder 集成**：
  - 打开文件或文件夹。
  - 在 Finder 中显示（Reveal in Finder）。
  - 打开所在目录（Open Containing Folder）。
  - 复制文件路径（单选/多选均支持）。
- **文件操作**：将选中的一个或多个结果复制或移动到指定目标文件夹，移动后自动刷新索引，操作失败（如目标已存在同名文件）会汇总提示错误信息。

## 应用外观与打包

- 原生 SwiftUI 界面，符合 macOS 人机交互规范，支持系统深色/浅色模式。
- 程序化生成的放大镜风格搜索图标，作为应用与 DMG 封面图标。
- 一键打包为 `.dmg` 安装镜像（`scripts/package-dmg.sh`），并配套 GitHub Actions 工作流 "Build macOS DMG"，可在 macOS Runner 上自动完成构建、签名占位与产物上传，无需本地 macOS 环境即可获得可分发的安装包。

## 技术实现要点

- 使用 Swift Package Manager 组织工程结构，核心逻辑分布在 `SearchViewModel`（业务与状态管理）、`FileScanner`（本地文件扫描）、`SpotlightSearch`（Spotlight 检索封装）、`SearchExpression`（查询语法解析与匹配）、`FileTypeCatalog`（文件类型分类）等模块。
- 遵循 Swift 6 严格并发模型，所有跨线程数据结构均标注 `Sendable`，检索与排序计算通过 `Task.detached` 在后台执行，避免阻塞主线程。
